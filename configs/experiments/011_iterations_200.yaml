experiment:
  name: 011_iterations_200
  parent: 001_best_solution
  purpose: check CatBoost iterations 200
  hypothesis: iteration cap 200 can reduce overfitting, but may underfit folds that improve late.

model:
  params:
    iterations: 200
